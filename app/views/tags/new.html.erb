<% provide(:page_title, "New Tag") %>
<% provide(:tag_active, "active") %>

<div class="row title" style="padding:20px;">
  <div class="col-12">
    <h1>New Tag</h1>
  </div>
</div>

<%= render 'form', tag: @tag %>
